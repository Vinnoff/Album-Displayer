<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="test.dev.albumdisplayer">

    <application
        android:name=".ABApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlbumDisplayer">
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="test.dev.albumdisplayer.di.initializer.LoggerInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="test.dev.albumdisplayer.di.initializer.KoinInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="test.dev.albumdisplayer.di.initializer.ThreeTenBPInitializer"
                android:value="androidx.startup" />
        </provider>
    </application>
</manifest>